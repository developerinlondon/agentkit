---
title: Skill catalogue
description: Every skill the kit installs, the group it belongs to, and which
  groups are opt-in.
sidebar:
  order: 3
slug: 0.4/reference/skills
---

import SkillTable from "../../../../components/SkillTable.astro";
import facts from "../../../../generated/kit-facts.json";

A skill is a set of instructions the agent loads when the work matches it. Unlike a hook, a skill
cannot refuse anything — it makes the work better when it is loaded and changes nothing when it is
not. Reach for a skill when the failure is "the output was worse than it should have been", and for
a hook when the failure is "that should not have been allowed".

This catalogue is generated from `skills/` and `skills/GROUPS` at build time. The descriptions are
the skills' own frontmatter, which is also what the agent sees when deciding whether to load one.

## Groups

A group decides whether a skill is installed. Anything with no group record belongs to `core`,
which always installs.

<table>
  <thead>
    <tr>
      <th>Group</th>
      <th>Selection</th>
      <th>What it covers</th>
    </tr>
  </thead>

  <tbody>
    {
        		facts.groups.map((group) => (
        			<tr>
        				<td>
        					<code>{group.id}</code>
        				</td>
        				<td>{group.explicit ? "explicit opt-in only" : "offered by default"}</td>
        				<td>{group.description}</td>
        			</tr>
        		))
        	}
  </tbody>
</table>

:::caution[An explicit group is consent-gated in both directions]
An explicit group is never offered by the interactive picker and is excluded from `--all`. It
installs only when you name it: `--with strict-review`. If it is not named on a later install run,
the installer **removes** the artifacts it previously installed — presence without a recorded
selection is not treated as consent.
:::

## Every skill

<SkillTable />
