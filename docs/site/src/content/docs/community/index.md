---
title: Community
description: Where the repository lives, how to file an issue, what the licence and CLA commit you to, and how to land a new skill or police unit.
sidebar:
  order: 1
---

|            |                                                      |
| ---------- | ---------------------------------------------------- |
| Repository | https://github.com/developerinlondon/agentkit        |
| Issues     | https://github.com/developerinlondon/agentkit/issues |
| Licence    | Apache License 2.0 — `LICENSE`, `NOTICE`             |
| CLA        | required for every contribution — `CLA.md`           |

That is the whole list. There is no chat server, forum, mailing list or code of conduct in this
repository, and this page will not invent one.

## Filing an issue

GitHub issues on the repository above. The repository ships no issue templates, so include what a
maintainer cannot reconstruct:

- Which client — OpenCode, Claude Code, Codex CLI or Grok CLI. A police unit has up to three
  implementations and they are deliberately not equivalent, so "the hook blocked it" is ambiguous
  until you say which one.
- Which unit, by name (`git-police`, `resource-police`, …) and the refusal text verbatim. The
  message is generated by the guard and identifies the branch of the code that fired.
- Platform. `bounded-run` and `agent-session` are Linux-only and are omitted elsewhere, so several
  behaviours differ by design rather than by fault.
- How it was installed: `install.sh --global`, a project install, `--claude-plugin`, and which
  `--with` kits.

## Licence

Apache License 2.0. `NOTICE` records the copyright as `agentkit, Copyright 2026 Nayeem Syed` and
declares one piece of third-party content:
`skills/product-intelligence/assets/brief-template.md` is derived from
[BMAD-METHOD](https://github.com/bmad-code-org/BMAD-METHOD), Copyright (c) 2025 BMad Code, LLC,
under the MIT License. The MIT text is reproduced in full in `NOTICE`.

## The CLA

Contributions require the Contributor License Agreement in `CLA.md`. Read it before your first pull
request, because it grants more than Apache-2.0 inbound would on its own.

- You keep the copyright in your contribution (§2).
- You grant the Project Owner a perpetual, worldwide, irrevocable copyright licence to use and
  distribute it **under any licence terms, including proprietary or commercial ones** (§2). In plain
  terms: the project can be relicensed, and your contribution comes with it.
- You grant a patent licence covering your contribution alone and in combination with the project.
  It terminates for any entity that files patent litigation alleging your contribution or the
  project infringes (§3).
- You represent that you are legally entitled to grant this; that where an employer has rights, it
  has given permission, waived them, or signed a separate agreement; that each contribution is your
  original creation; and that you disclose any third-party licence or restriction you are aware of
  (§4).
- Work that is not your original creation is submitted **separately** from a contribution, with its
  source and restrictions detailed and marked
  `Submitted on behalf of a third-party: [named here]` (§5).
- You owe no support, and your contributions are provided AS IS without warranties (§6).
- You agree to notify the Project Owner if anything later makes these representations inaccurate
  (§7).

Signing is a bot flow, not paperwork: open a pull request, the CLA Assistant posts a comment, click
its link and agree with your GitHub account. Once per contributor, not once per PR.

## Contributing a skill

A skill is a directory, not a registration. From `README.md`:

1. Skills follow the [skills.sh](https://skills.sh) / [agentskills.io](https://agentskills.io)
   standard.
2. Each lives in `skills/<name>/SKILL.md` with optional `references/` and `scripts/` subdirectories.

Then two things that are easy to miss:

- **Kit membership.** A skill with no record in `skills/KITS` belongs to `core` and therefore
  installs for everyone. If it should be opt-in, add a membership line naming a declared kit. A
  membership line whose kit is never declared, or a skill named in two kits, aborts the
  installer before it writes anything.
- **The plugin mirror.** `skills/` and `plugins-cc/agentkit/skills/` must stay byte-identical.
  Regenerate with `scripts/sync-cc-plugin.sh` and commit the result; never edit the mirror by hand.

## Contributing a police unit

A police unit is a policy, so contributing one means implementing it once per mechanism that can
hold it, and being explicit about where it cannot reach:

| Mechanism                       | File                          | Reaches               |
| ------------------------------- | ----------------------------- | --------------------- |
| `PreToolUse`/`PostToolUse` hook | `hooks/claude/<name>.sh`      | Claude Code, Grok CLI |
| OpenCode plugin                 | `plugins/<name>.ts`           | OpenCode              |
| Codex argv-prefix policy        | `policies/codex/<name>.rules` | Codex CLI             |

Partial coverage is acceptable and normal — `version-police` is OpenCode-only, `mr-police`,
`pages-police` and `issue-police` are hook-only, `delegation-police` is Codex-only. What is not acceptable is implying
parity that does not exist.

Points where a new unit usually goes wrong:

- **A `PostToolUse` hook must exit 2 to be heard.** Claude Code discards its stderr on exit 0, so a
  hook that finds a violation and exits 0 has run and told nobody.
- **Choose the failure direction deliberately.** A detection guard that cannot run should say so
  loudly and allow (`resource-police` prints `DISABLED — missing …`); a gate that cannot evaluate
  should deny (`review-police` refuses the merge when `jq` is absent). Both patterns exist in the
  tree; pick one and justify it.
- **Read config from the right place.** Hooks read `${XDG_CONFIG_HOME:-~/.config}/agentkit/config.yaml`.
  Only `review-profile` reads a repo-level `.agentkit/config.yaml`.
- **Give every refusal an exception.** The convention is an `AGENTKIT_ALLOW_*=1` prefix on one
  command, matched both from the environment and inline in the command text — a prefix assignment
  never reaches the hook's own environment.
- **Platform-gate with metadata, not filenames.** An artifact that only works on one host declares
  `# agentkit:platforms linux` in its first 15 lines; the installer skips and removes accordingly.

## Contributing a rule, an instruction or a tool

| Kind            | Where                    | Contract                                            |
| --------------- | ------------------------ | --------------------------------------------------- |
| Rule            | `rules/<name>.md`        | frontmatter `globs:` for auto-loading               |
| Instruction     | `instructions/<name>.md` | wrapped in `<!-- agentkit:<name>:start -->` markers |
| OpenCode plugin | `plugins/<name>.ts`      | implements the OpenCode plugin API                  |
| Tool            | `tools/<name>`           | standalone executable script (bash or Python)       |

## Before you open the pull request

The repository ships its own build gate, and it is deterministic — nothing in it calls a model.

```sh
scripts/preflight                 # everything touched since origin/main
scripts/preflight --slice review  # also run one test slice
bun run test:full                 # the full suite, run in parallel
```

`preflight` reads the touched set from git — committed, staged, unstaged and untracked — and exits 1
on any finding. `BUILD-DISCIPLINE.md` catalogues fourteen defect classes, each found by hand in a
review of this repository more than once, and each carrying a **Caught by** line. Nine name a
mechanical check — `preflight` itself or a helper it calls (`scripts/bash-status-check`,
`scripts/lib/test-verdict.sh`, `scripts/check-test-slices.ts`). Four say "Nothing static" outright and
hand the class to `scripts/mutate` or to a reviewer. One prescribes a practice rather than naming a
check. Two will bite a first contribution: a new test file that no slice in
`scripts/check-test-slices.ts`'s `TEST_SLICES` claims, and plugin-mirror drift.

For load-bearing lines, `scripts/mutate` breaks the line on purpose and reports whether the tests
notice:

```sh
scripts/mutate --file plugins/thing.ts \
  --replace 'escapeHtml(name)' --with 'name' \
  --test 'bun test tests/thing.test.ts'
```

It exits 0 CAUGHT, 1 SURVIVED, 2 usage, 3 harness fault, and restores from a scratch copy with a
digest check rather than relying on git to undo itself. A test that cannot be made to fail is not
evidence.
