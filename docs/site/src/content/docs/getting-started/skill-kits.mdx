---
title: Skill kits
description: How skills are partitioned into kits, and exactly when the installer asks.
sidebar:
  order: 2
---

import KitTable from "../../../components/KitTable.astro";

Skills are partitioned by a plain-text manifest, `skills/KITS`. A skill with no record belongs
to `core`, which always installs. Adding a kit is a manifest entry — the installer, the Claude
plugin generator and the tests all read that one file rather than hard-coding names.

The kits declared today, read out of the manifest at build time:

<KitTable />

`core` always installs and cannot be deselected. `product` is `product-intelligence` and
`product-review`. The `adversarial-review` kit is named after its centrepiece skill of the same
name, and adds the merge gate's hooks, tools and the `evidence-gated-review` instruction file. `advisory-review` carries no skill at all — it gates
one instruction file, `review-discipline.md`, which asks for a non-authoring reviewer pass before a
merge. A kit with no skills publishes no Claude Code plugin, because skills are the whole payload
of a generated kit plugin.

The two review kits are AgentKit's **two review modes**, and they are independent: enable either,
both, or neither. `advisory-review` asks; `adversarial-review` refuses.

```sh
./agentkit/install.sh --global                        # core only, or last time's answer
./agentkit/install.sh --global --with product         # core + the product kit
./agentkit/install.sh --global --all                  # every non-explicit kit
./agentkit/install.sh --global --with adversarial-review     # the only way to get the gate
./agentkit/install.sh --global --with advisory-review   # the advisory reviewer-pass instruction
./agentkit/install.sh --global --with advisory-review --with adversarial-review   # both review modes
```

## Explicit kits are consent-gated

A kit marked `explicit` in the manifest behaves differently from a merely optional one, and the
difference is the whole point:

- The interactive wizard never offers it. A `y` at a prompt is too easy to give without reading
  what it wires in.
- `--all` does not include it.
- When a run does **not** select it, the installer **removes** its previously installed skills,
  hooks, tools and prompt wiring.

That last rule is deliberate: presence without recorded selection is not consent. Finding the
merge gate on disk is not evidence that anybody chose to be gated.

A remembered selection still counts as selected, so a bare `install.sh --global` after
`--with adversarial-review` **keeps** it. To take it away, drop it explicitly:

```sh
./agentkit/install.sh --global --without adversarial-review
```

Observed removal on that run — the skill, both hooks, both tools, the Codex prompt and hook
wiring, the `CLAUDE.md` prompt block and the OpenCode `instructions[]` entry all go:

```text
[skills] Removing (explicit kit 'adversarial-review' not selected): adversarial-review
[claude] Removed prompt block (evidence-gated-review.md): …/.claude/CLAUDE.md
[opencode] Removed global prompt: …/.config/opencode/opencode.json
[claude] Removing hook (adversarial-review kit not selected): review-police.sh
[tools] Removing (adversarial-review kit not selected): review-gate
[codex] Removed review gate hooks (adversarial-review kit not selected).
```

:::caution
The review machinery is opt-in. Neither `--all` nor the picker installs it. If you want the merge
gate, you have to type `--with adversarial-review`.
:::

## What a global install remembers

A global install records the chosen kits in `~/.agentkit/kits`, so a later bare
`install.sh --global` upgrades the same set with no flags to re-pass. `--with` adds to that set
rather than replacing it; `--without <kit>` drops one.

Deleting a line stops the kit being **selected**. For a normal optional kit it never deletes
files already on disk — an already-installed skill from an unselected kit is still refreshed,
and the installer says so. (An explicit kit is the exception above: deselecting it removes its
artifacts.)

An unknown kit name left in that file is reported on stderr and ignored, not taken as a
selection.

Project installs take kits per invocation and persist nothing.

## When it asks, and when it stays quiet

On a global install with a real terminal and no remembered answer, the installer asks once per
optional non-explicit kit. The question goes to `/dev/tty`, never to stdout, so it cannot
corrupt a piped log.

```text
[kits] Optional skill kits — core installs either way.
[kits]   product: Product-model skills: evidence-backed briefs and product review
[kits]   Install product? [y/N]
```

The default is **decline**. Only `y`, `Y` or `yes` in any casing accept; a bare Enter, an `n`,
garbage, or a failed read all decline and the install continues.

Every guard fails towards silence. The wizard does not run when any of these holds:

| Condition                                                 | Why                                                                                        |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| not `--global`                                            | Project installs persist nothing, so the answer could not be kept and every run would ask. |
| stdin **or** stdout not a tty                             | Output routed into a pipe or a log means an operator who cannot see the question.          |
| `CI` or `AGENTKIT_SKIP_PROMPT` set to any non-empty value | A pty is not evidence of a person — Docker and Jenkins hand out ptys.                      |
| `--no-prompt` or `--all`                                  | An explicit instruction about kits, so there is nothing left to ask.                     |
| any `--with` / `--without`                                | You already answered on the command line.                                                  |
| `~/.agentkit/kits` exists                               | The question was already put. An **empty** file counts as the recorded answer "core only". |

If the guards pass but `/dev/tty` cannot be opened, the installer says so on stderr and installs
core:

```text
[kits] No controlling terminal — installing core only.
[kits] Use --with <kit> to add an optional kit.
```

An unanswered question does not degrade to a decline — it stops the install until something kills
it. That is why every condition above resolves towards not asking.

## Manifest validation

The installer validates `skills/KITS` before it writes anything. Each of these aborts the run:

- a membership line with no kit name (it would silently fall through to `core` and ship that
  skill to everyone)
- a skill named in two kits (the bash reader takes the first match, the TypeScript reader takes
  the last — the two would ship different sets)
- an `explicit` marker for a kit that was never declared
- a membership naming a kit that was never declared (that skill would be uninstallable by any
  flag)

CRLF line endings are rejected loudly by the bash reader. Keep the file LF.

What a skill actually is, and why one can never do a hook's job:
[Skills](/docs/concepts/skills/).
