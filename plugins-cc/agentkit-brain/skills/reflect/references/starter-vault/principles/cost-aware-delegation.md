# Cost-Aware Delegation

Match the tool to the job's actual difficulty. Deterministic transforms run as
code, never through a model. Mechanical work goes to cheap fast models; deep
analysis and adversarial verification earn expensive ones. Parallelism is for
independent work — a dependency chain gains nothing from a fleet.
