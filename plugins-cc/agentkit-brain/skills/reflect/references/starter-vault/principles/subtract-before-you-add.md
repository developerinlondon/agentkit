# Subtract Before You Add

Before adding code, config, or process, look for something to remove. Most
systems accumulate; few prune. A fix that deletes the broken path outranks one
that adds a compensating layer on top of it. When replacing behaviour, the old
path's removal is part of the change, not a follow-up.
